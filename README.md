# Moved to Codeberg

This project has migrated to [Codeberg](https://codeberg.org/dlford/scan-to-nextcloud).

Please update your docker image to point to `codeberg.org` instead of `ghcr.io` starting with version 1.0.1, for example: `codeberg.org/dlford/tv_grab_zz_sdjson:latest`.
